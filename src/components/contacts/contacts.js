import React from 'react';

import './contacts.css';

const Contacts = () => {
    return (
        <div className="boxContacts" id="contacts" >
            <h1>Contacts</h1>
        </div>
    );
};

export default Contacts;